This module is empty and its purpose is to make it easy to include all
other EPS modules in a JEE application.

For JSE applications, use eps-dist.

eps-jboss-module-dist will not include needed service framework implementation modules, 
instead the service framework JBoss module should be used.

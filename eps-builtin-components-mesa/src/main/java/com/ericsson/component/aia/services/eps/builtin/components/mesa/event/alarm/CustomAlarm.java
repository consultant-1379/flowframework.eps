package com.ericsson.component.aia.services.eps.builtin.components.mesa.event.alarm;

/**
 * Alarm that does not conform to standard specification. This interface should
 * be implemented if future features require it.
 */
public interface CustomAlarm extends Alarm {
}
